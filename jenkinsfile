pipeline{
  agent any
   stages{
     stage('build'){
       steps{ 
              echo 'running build automation'
              sh './gradlew build -no --daemoun'
              archivesArtifacts artifacts: 'dist/trainshedule2.zip'
              }
        }
    }
}
